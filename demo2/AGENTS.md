# Demo 2 — Agent Instructions

Scope: `demo2/**`.

This folder is a simpler demo focused on inputs + standardized “cards” views.

- Keep it small and readable; it should showcase the format, not the UI framework.
- Prefer JSON `view` blocks for portability; YAML is also accepted by the parser.
